# Pre- and Post-processors

Отличная работа, но есть замечание, детали ниже.

## Task 01

Плюсы:
- Использовала миксины по максимуму;
- Использовала перменные по максимуму (*map*);
- Использовала циклы;
- Использовала `@extend`;
- Использовала файловую структуру.

Минусы:
- Почему вместо таких миксинов как представлено ниже (*без входных данных*), не использовать `@extend` (*не будет дупликации кода на выходе и весить итоговый файл будет меньше соответсвенно*)?
```scss
@mixin flex{
  display: flex;
  justify-content: space-between;
  flex-wrap: nowrap;
  align-items: flex-start;
}
```

## Task 02

По этому таску замечаний нет.
